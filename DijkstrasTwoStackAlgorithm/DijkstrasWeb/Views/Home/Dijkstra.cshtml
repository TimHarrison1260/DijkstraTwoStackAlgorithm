@model DijkstrasWeb.Models.DijkstrasTwoStackAlgorithmModel

@{
    ViewBag.Title = "Interpret";
}

<h2>Interpret</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DijkstrasTwoStackAlgorithmModel</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Expression)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Expression)
            @Html.ValidationMessageFor(model => model.Expression)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Result)
        </div>
        <div class="editor-field">
            @Html.DisplayFor(model => model.Result)
@*            @Html.EditorFor(model => model.Result)
            @Html.ValidationMessageFor(model => model.Result)*@
        </div>

        <p>
            <input type="submit" value="Interpret" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
